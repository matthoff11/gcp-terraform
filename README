#Install ansible ubuntu
apt-add-repository ppa:ansible/ansible
apt install ansible


update your key

gcpiam.json

update your project id

terraform.tfvars

run

ansible-playbook automate.yml --become

run

source /etc/profile.d/zzz-ansible-lab.sh
